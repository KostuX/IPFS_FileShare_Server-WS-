export class Client {
    id = null
    key = null
    connection = null
    constructor(id, key, connection){
        this.id = id
        this.key = key
        this.connection = connection
        console.log("Client Class created")
    }

  

}